{
  "name": "memoryos",
  "version": "0.1.1",
  "private": true,
  "description": "A cinematic spatial memory operating system.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm-run-all --print-label --parallel dev:frontend dev:backend",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint",
    "typecheck": "npm --workspace frontend run typecheck && npm --workspace backend run typecheck",
    "db:generate": "npm --workspace backend run db:generate",
    "db:migrate": "npm --workspace backend run db:migrate"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
