# MemoryOS API v0.1.0

This archive contains the compiled Fastify API, Prisma schema/migrations, Dockerfile, package metadata, and environment example.

Install production dependencies before running: npm ci --omit=dev; npx prisma migrate deploy; npm start

