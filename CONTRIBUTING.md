# Contributing

1. Create focused changes with matching frontend/backend tests where risk warrants it.
2. Keep the UI cinematic, matte, and functional. Avoid dead controls.
3. Run `npm run typecheck` and `npm run build` before opening a PR.
4. Never commit secrets, uploads, or generated build artifacts.
